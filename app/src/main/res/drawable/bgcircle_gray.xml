<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- Light gray color for unselected state -->
    <solid android:color="#D3D3D3" />
    <!-- Optional: Define a fixed size (remove if your view already defines its size) -->
    <size
        android:width="50dp"
        android:height="50dp" />
</shape>
