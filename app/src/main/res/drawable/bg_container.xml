<!-- res/drawable/bg_container.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- A semi-transparent background color (black at 30% opacity) -->
    <solid android:color="#4D000000" />

    <!-- Rounded corners (adjust dp as needed) -->
    <corners android:radius="16dp" />

    <!-- Optional padding inside the shape (you can also just do this in the layout) -->
    <!-- <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp" /> -->

</shape>
